<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wish">

<!-- 찜 여부 확인 -->
<select id="findByUserAndProduct" resultType="com.kh.jipshop.wish.model.vo.WishDTO">
    SELECT *
    FROM WISHLIST
    WHERE MEMBER_NO = #{memberNo}
    AND PRODUCT_ID = #{productId}
</select>

<!-- 찜 추가 -->
<insert id="insertWish">
    INSERT INTO WISHLIST
    (WISH_ID, MEMBER_NO, PRODUCT_ID)
    VALUES
    (SEQ_WISH.NEXTVAL, #{memberNo}, #{productId})
</insert>

<!-- 찜 삭제 -->
<delete id="deleteWish">
    DELETE FROM WISHLIST
    WHERE MEMBER_NO = #{memberNo}
    AND PRODUCT_ID = #{productId}
</delete>

<!-- 전체 삭제 -->
<delete id="deleteAll">
    DELETE FROM WISHLIST
    WHERE MEMBER_NO = #{memberNo}
    AND PRODUCT_ID IN
    <foreach item="id" collection="ids" open="(" separator="," close=")">
        #{id}
    </foreach>
</delete>

<!-- 찜 리스트 -->
<select id="selectWishList" resultType="com.kh.jipshop.wish.model.vo.WishDTO">
    SELECT W.*, P.PRODUCT_NAME, P.PRODUCT_PRICE, P.PRODUCT_PHOTO1
    FROM WISHLIST W
    JOIN PRODUCT P ON W.PRODUCT_ID = P.PRODUCT_ID
    WHERE W.MEMBER_NO = #{memberNo}
</select>

<!-- 찜 ID 리스트 -->
<select id="selectWishIds" resultType="int">
    SELECT PRODUCT_ID
    FROM WISHLIST
    WHERE MEMBER_NO = #{memberNo}
</select>

</mapper>