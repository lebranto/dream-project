<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mypage">
<select id="orderList" resultType="orders"> 
SELECT * 
FROM ORDERS 
WHERE MEMBER_NO =#{memberNo} 
</select>

<select id="orderListPeriod" resultType="orders">
SELECT *
FROM ORDERS
WHERE MEMBER_NO= #{memberNo}
AND ORDER_DATE >= SYSDATE - #{period}
</select>


<select id="orderListDate" resultType="orders">

SELECT *
FROM ORDERS
WHERE MEMBER_NO= #{memberNo}
AND ORDER_DATE BETWEEN #{startDate} AND #{endDate}
</select>


<select id="recentlyList" resultType="recentlyviewed"> 
SELECT * 
FROM RECENTLY_VIEWED 
WHERE MEMBER_NO =#{memberNo} 

</select>

<select id="recentlyListPeriod" resultType="recentlyviewed"> 
SELECT * 
FROM RECENTLY_VIEWED 
WHERE MEMBER_NO =#{memberNo} 
AND VIEWED_DATE >= SYSDATE - #{period}

</select>


<select id="recentlyListDate" resultType="recentlyviewed">

SELECT * 
FROM RECENTLY_VIEWED 
WHERE MEMBER_NO =#{memberNo} 
AND VIEWED_DATE BETWEEN #{startDate} AND #{endDate}

</select>

<select id="inquiryList" resultType="MyInqury"> 

SELECT * 
FROM INQUIRY
WHERE MEMBER_NO =#{memberNo} 
</select>



</mapper>